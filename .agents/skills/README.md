# Repo skills

Project-local Agent Skills for this repository (Agent Skills standard:
`SKILL.md` + optional `scripts/`, `references/`).

Discovered by Cursor, pi, and Codex from `.agents/skills/`; `.opencode/skills`
is symlinked here for OpenCode. Keep tugboat-mobile skills in this repo —
never in the workspace-level `~/work/skills/` (that is for cross-repo
workflows only). Skills must be self-contained: reference this repo's `docs/`
and `docs/glossary/INDEX.md`, never the pmkit-docs archive.
