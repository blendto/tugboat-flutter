Privacy rules for native capture live in
[native-capture-contracts.md](../architecture/native-capture-contracts.md).

Mask-before-encode ownership is summarized in [pipeline.md](pipeline.md).

Host-side tests cover the Dart ↔ native contract. Physical-device privacy and
performance rows remain an internal lab gate. Do not enable native capture as
the default while those rows are open.
