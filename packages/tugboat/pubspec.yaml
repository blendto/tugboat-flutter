name: tugboat
description: >-
  Screenshot-based session replay with compact interaction anchors for Tugboat.
version: 0.1.0

environment:
  sdk: ^3.9.2
  flutter: ">=3.35.0"

resolution: workspace

dependencies:
  crypto: ^3.0.6
  flutter:
    sdk: flutter
  http: ^1.4.0
  http_parser: ^4.1.2
  image: ^4.5.4

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
