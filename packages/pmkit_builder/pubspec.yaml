name: pmkit_builder
description: Optional build-time widget name generation for the PMKit Flutter SDK.
version: 0.1.0

environment:
  sdk: ^3.9.2

resolution: workspace

dependencies:
  analyzer: ">=6.0.0 <9.0.0"
  build: ^4.0.1
  glob: ^2.1.2

dev_dependencies:
  build_test: ^3.4.1
  lints: ^5.1.1
  test: ^1.25.8
