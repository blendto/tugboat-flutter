# React Native adapter

Future npm package `@tugboat/react-native`. Not implemented.

This adapter will consume the published Android AAR and Apple CocoaPod.
Raw pixels must never enter JavaScript. There is no npm workspace in this
repository yet.
