# Apple capture runtime

Swift module and CocoaPod `TugboatCaptureRuntime`. Milestone 2.

`Package.swift` and `TugboatCaptureRuntime.podspec` will live at the
repository root so Swift Package Manager can resolve a remote package.
